<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:VivaVoz.ViewModels"
        x:Class="VivaVoz.Views.OnboardingWindow"
        Title="Welcome to VivaVoz"
        Width="560"
        Height="500"
        CanResize="False"
        WindowStartupLocation="CenterScreen"
        Icon="avares://VivaVoz/Assets/vivavoz.ico">

  <Grid RowDefinitions="Auto,*,Auto">

    <!-- Step indicator bar -->
    <Border Grid.Row="0" Padding="24,14" BorderThickness="0,0,0,1" BorderBrush="#1FFFFFFF">
      <Grid ColumnDefinitions="*,Auto">
        <TextBlock Grid.Column="0"
                   Text="Getting Started"
                   FontSize="12"
                   Opacity="0.5"
                   VerticalAlignment="Center" />

        <!-- Simple step counter -->
        <TextBlock Grid.Column="1"
                   FontSize="12"
                   Opacity="0.5"
                   VerticalAlignment="Center"
                   Text="{Binding CurrentStepDisplay, StringFormat='Step {0} of 4'}" />
      </Grid>
    </Border>

    <!-- Step content area -->
    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
      <Grid>

        <!-- ── Step 1: Welcome ──────────────────────────────────────────── -->
        <StackPanel IsVisible="{Binding IsStep1}"
                    Spacing="20"
                    Margin="40,32"
                    HorizontalAlignment="Stretch">

          <Image Source="avares://VivaVoz/Assets/vivavoz.ico"
                 Width="72" Height="72"
                 HorizontalAlignment="Center" />

          <TextBlock Text="Welcome to VivaVoz!"
                     FontSize="28"
                     FontWeight="Bold"
                     HorizontalAlignment="Center" />

          <TextBlock Text="Your voice, alive."
                     FontSize="16"
                     Opacity="0.65"
                     HorizontalAlignment="Center" />

          <StackPanel Spacing="16" Margin="0,12,0,0">
            <Grid ColumnDefinitions="32,*" ColumnSpacing="12">
              <TextBlock Grid.Column="0" Text="🎙" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" />
              <StackPanel Grid.Column="1" Spacing="2">
                <TextBlock Text="Record" FontWeight="SemiBold" />
                <TextBlock Text="Capture your voice with a single hotkey from anywhere."
                           FontSize="12" Opacity="0.6" TextWrapping="Wrap" />
              </StackPanel>
            </Grid>

            <Grid ColumnDefinitions="32,*" ColumnSpacing="12">
              <TextBlock Grid.Column="0" Text="✏️" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" />
              <StackPanel Grid.Column="1" Spacing="2">
                <TextBlock Text="Transcribe" FontWeight="SemiBold" />
                <TextBlock Text="Instantly convert speech to text using Whisper AI — on-device, no cloud."
                           FontSize="12" Opacity="0.6" TextWrapping="Wrap" />
              </StackPanel>
            </Grid>

            <Grid ColumnDefinitions="32,*" ColumnSpacing="12">
              <TextBlock Grid.Column="0" Text="📋" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" />
              <StackPanel Grid.Column="1" Spacing="2">
                <TextBlock Text="Paste" FontWeight="SemiBold" />
                <TextBlock Text="Automatically copy transcribed text to clipboard and paste anywhere."
                           FontSize="12" Opacity="0.6" TextWrapping="Wrap" />
              </StackPanel>
            </Grid>
          </StackPanel>

          <TextBlock Text="Let's set up VivaVoz in a few quick steps."
                     FontSize="13"
                     Opacity="0.55"
                     HorizontalAlignment="Center"
                     Margin="0,8,0,0" />
        </StackPanel>

        <!-- ── Step 2: Model Selection ──────────────────────────────────── -->
        <StackPanel IsVisible="{Binding IsStep2}"
                    Spacing="16"
                    Margin="40,32">

          <TextBlock Text="Choose a Transcription Model"
                     FontSize="22"
                     FontWeight="Bold" />

          <TextBlock Text="Base is pre-selected and works great. Download Medium or Large for better accuracy on complex or accented speech."
                     FontSize="13"
                     Opacity="0.65"
                     TextWrapping="Wrap" />

          <ItemsControl ItemsSource="{Binding Models}">
            <ItemsControl.ItemTemplate>
              <DataTemplate DataType="{x:Type vm:ModelItemViewModel}">
                <Border Padding="0,8"
                        BorderThickness="0,0,0,1"
                        BorderBrush="#10FFFFFF">
                  <Grid ColumnDefinitions="*,Auto,Auto,Auto,Auto">

                    <StackPanel Grid.Column="0" VerticalAlignment="Center" Spacing="2">
                      <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="{Binding DisplayName}" FontWeight="Medium" />
                        <TextBlock Text="{Binding ExpectedSize}" FontSize="11" Opacity="0.55" VerticalAlignment="Center" />
                      </StackPanel>
                      <TextBlock Text="{Binding StatusText}" FontSize="11" Opacity="0.6" />
                    </StackPanel>

                    <ProgressBar Grid.Column="1"
                                 Value="{Binding DownloadProgress}"
                                 Minimum="0" Maximum="1"
                                 IsVisible="{Binding IsDownloading}"
                                 Width="80" Height="4"
                                 VerticalAlignment="Center"
                                 Margin="8,0" />

                    <Button Grid.Column="2"
                            Content="Cancel"
                            Command="{Binding CancelDownloadCommand}"
                            IsVisible="{Binding CanCancel}"
                            Padding="8,4"
                            Margin="4,0,0,0" />

                    <Button Grid.Column="2"
                            Content="Download"
                            Command="{Binding DownloadCommand}"
                            IsVisible="{Binding CanDownload}"
                            Padding="8,4"
                            Margin="4,0,0,0" />

                    <Button Grid.Column="3"
                            Content="Use"
                            Command="{Binding SelectCommand}"
                            IsVisible="{Binding CanSelect}"
                            Padding="8,4"
                            Margin="4,0,0,0" />

                    <TextBlock Grid.Column="3"
                               Text="Active"
                               IsVisible="{Binding IsSelected}"
                               VerticalAlignment="Center"
                               FontSize="11"
                               Opacity="0.7"
                               Margin="4,0,0,0" />

                    <Button Grid.Column="4"
                            Content="Delete"
                            Command="{Binding DeleteCommand}"
                            IsVisible="{Binding CanDelete}"
                            Padding="8,4"
                            Margin="4,0,0,0" />
                  </Grid>
                </Border>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>

          <TextBlock Text="You can always download more models later in Settings."
                     FontSize="12"
                     Opacity="0.45"
                     Margin="0,4,0,0" />
        </StackPanel>

        <!-- ── Step 3: Test Recording ────────────────────────────────────── -->
        <StackPanel IsVisible="{Binding IsStep3}"
                    Spacing="20"
                    Margin="40,32">

          <TextBlock Text="Test Your Microphone"
                     FontSize="22"
                     FontWeight="Bold" />

          <TextBlock Text="Press Record, say a few words, then press Stop. VivaVoz will transcribe it so you can see it working."
                     FontSize="13"
                     Opacity="0.65"
                     TextWrapping="Wrap" />

          <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,8,0,0">
            <Button Content="⏺  Record"
                    Command="{Binding StartTestRecordingCommand}"
                    Padding="16,8"
                    FontSize="14" />

            <Button Content="⏹  Stop"
                    Command="{Binding StopTestRecordingCommand}"
                    Padding="16,8"
                    FontSize="14" />
          </StackPanel>

          <Border IsVisible="{Binding IsTestRecording}"
                  Background="#1800CC00"
                  BorderBrush="#3300BB00"
                  BorderThickness="1"
                  CornerRadius="6"
                  Padding="12,8">
            <TextBlock Text="🔴  Recording — speak now"
                       FontSize="13" />
          </Border>

          <Border IsVisible="{Binding IsTestTranscribing}"
                  Background="#18FFFFFF"
                  BorderBrush="#18FFFFFF"
                  BorderThickness="1"
                  CornerRadius="6"
                  Padding="12,8">
            <TextBlock Text="⏳  Transcribing..."
                       FontSize="13"
                       Opacity="0.75" />
          </Border>

          <Border IsVisible="{Binding HasTestRecorded}"
                  Background="#12FFFFFF"
                  BorderBrush="#18FFFFFF"
                  BorderThickness="1"
                  CornerRadius="6"
                  Padding="14,12">
            <StackPanel Spacing="6">
              <TextBlock Text="Transcription result:"
                         FontSize="11"
                         Opacity="0.5" />
              <TextBlock Text="{Binding TestTranscript}"
                         FontSize="14"
                         TextWrapping="Wrap" />
            </StackPanel>
          </Border>

          <TextBlock Text="You can skip this step — your microphone can be configured anytime in Settings."
                     FontSize="12"
                     Opacity="0.45"
                     TextWrapping="Wrap"
                     Margin="0,4,0,0" />
        </StackPanel>

        <!-- ── Step 4: Hotkey Setup ──────────────────────────────────────── -->
        <StackPanel IsVisible="{Binding IsStep4}"
                    Spacing="20"
                    Margin="40,32">

          <TextBlock Text="Set Your Recording Hotkey"
                     FontSize="22"
                     FontWeight="Bold" />

          <TextBlock Text="Use this hotkey to start and stop recording from anywhere — even when VivaVoz is running in the background."
                     FontSize="13"
                     Opacity="0.65"
                     TextWrapping="Wrap" />

          <Border Background="#12FFFFFF"
                  BorderBrush="#18FFFFFF"
                  BorderThickness="1"
                  CornerRadius="8"
                  Padding="20,16">
            <StackPanel Spacing="12">
              <TextBlock Text="Current hotkey:"
                         FontSize="12"
                         Opacity="0.5" />
              <StackPanel Orientation="Horizontal" Spacing="16" VerticalAlignment="Center">
                <TextBlock Text="{Binding HotkeyDisplayText}"
                           FontSize="20"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center" />
                <Button Content="Set"
                        Command="{Binding StartSetHotkeyCommand}"
                        IsEnabled="{Binding !IsListeningForHotkey}"
                        Padding="12,6" />
              </StackPanel>

              <TextBlock IsVisible="{Binding IsListeningForHotkey}"
                         Text="Press your desired key combination now..."
                         FontSize="12"
                         Opacity="0.65"
                         FontStyle="Italic" />
            </StackPanel>
          </Border>

          <StackPanel Spacing="6" Margin="0,4,0,0">
            <TextBlock Text="• Press the hotkey once to start recording, again to stop."
                       FontSize="12" Opacity="0.55" TextWrapping="Wrap" />
            <TextBlock Text="• Works globally — even when other apps are in focus."
                       FontSize="12" Opacity="0.55" TextWrapping="Wrap" />
            <TextBlock Text="• You can change this anytime in Settings."
                       FontSize="12" Opacity="0.55" TextWrapping="Wrap" />
          </StackPanel>

          <TextBlock Text="You're all set! Click Finish to start using VivaVoz."
                     FontSize="14"
                     FontWeight="Medium"
                     Opacity="0.8"
                     Margin="0,8,0,0" />
        </StackPanel>

      </Grid>
    </ScrollViewer>

    <!-- Navigation buttons -->
    <Border Grid.Row="2"
            Padding="24,14"
            BorderThickness="0,1,0,0"
            BorderBrush="#1FFFFFFF">
      <Grid ColumnDefinitions="Auto,*,Auto,Auto">

        <Button Grid.Column="0"
                Content="← Back"
                Command="{Binding PreviousCommand}"
                IsVisible="{Binding CanGoPrevious}"
                Padding="16,8" />

        <Button Grid.Column="2"
                Content="Next →"
                Command="{Binding NextCommand}"
                IsVisible="{Binding CanGoNext}"
                Padding="16,8"
                Margin="0,0,8,0" />

        <Button Grid.Column="3"
                Content="Finish ✓"
                Command="{Binding FinishCommand}"
                IsVisible="{Binding IsLastStep}"
                Padding="16,8" />
      </Grid>
    </Border>

  </Grid>
</Window>
